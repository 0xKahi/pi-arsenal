## REMOVED Requirements

### Requirement: p2p_hub config section with disabled default
**Reason**: The unpublished p2p-hub capability is replaced by the corresponding p2p-council capability and vocabulary.
**Migration**: Use `p2p-council-config` and its p2p-council configuration, command, storage, and terminology as applicable.

### Requirement: No observable behavior while disabled before activation
**Reason**: The unpublished p2p-hub capability is replaced by the corresponding p2p-council capability and vocabulary.
**Migration**: Use `p2p-council-config` and its p2p-council configuration, command, storage, and terminology as applicable.

### Requirement: One-time lazy activation
**Reason**: The unpublished p2p-hub capability is replaced by the corresponding p2p-council capability and vocabulary.
**Migration**: Use `p2p-council-config` and its p2p-council configuration, command, storage, and terminology as applicable.

### Requirement: Runtime guards and active disconnect after disable
**Reason**: The unpublished p2p-hub capability is replaced by the corresponding p2p-council capability and vocabulary.
**Migration**: Use `p2p-council-config` and its p2p-council configuration, command, storage, and terminology as applicable.
